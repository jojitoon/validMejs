{
  "name": "valid_me_js",
  "version": "1.0.9",
  "description": "a validator middleware",
  "main": "./dist/index.js",
  "repository": "https://github.com/JojiAndela/validMejs.git",
  "author": "jojitoon",
  "license": "MIT",
  "scripts": {
    "test": "nyc mocha --require @babel/register --exit",
    "watch": "nyc mocha --require @babel/register --watch",
    "coverage": "nyc --reporter=lcov --reporter=text-lcov npm test",
    "prepublish": "babel ./src --out-dir dist"
  },
  "dependencies": {
    "validator": "^10.9.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/node": "^7.0.0",
    "@babel/polyfill": "^7.0.0",
    "@babel/preset-env": "^7.1.6",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0"
  }
}
